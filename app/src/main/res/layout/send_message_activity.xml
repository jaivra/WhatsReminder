<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:clipChildren="false">
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <include
        android:id="@+id/toolbar"
        layout="@layout/my_toolbar3"/>

        <androidx.appcompat.widget.AppCompatTextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center_horizontal"
            android:textSize="40sp"
            android:text="Alessando Sisto"/>

        <androidx.appcompat.widget.AppCompatTextView
            android:layout_margin="10dp"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center_horizontal"
            android:textSize="20sp"
            android:text="Questo è il messaggio da inviare ad Alessandro Sisto"/>

    </LinearLayout>
    <com.ncorti.slidetoact.SlideToActView
        android:id="@+id/slider"
        android:layout_margin="20dp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:area_margin="-8dp"
        app:animation_duration="250"
        app:outer_color="@color/slidetoact_white"
        app:inner_color="@color/colorPrimary"
        app:text_appearance="@font/handlee"
        app:text="Send Message"
        app:text_size="20sp"
        android:layout_alignParentBottom="true"
        app:slider_locked="false" />

</RelativeLayout>
